import React from 'react';
import {SafeAreaView, Text} from 'react-native';

const Router = () => {
  return (
    <SafeAreaView>
      <Text>Router</Text>
    </SafeAreaView>
  );
};

export default Router;
